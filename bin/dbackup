#!/usr/bin/env python3

# This is a simple wrapper script for loading the dbackup module,
# instantiating the DBackup main app, and run it

import dbackup

if __name__ == '__main__':
    app = dbackup.DBackup()
    app.run()
